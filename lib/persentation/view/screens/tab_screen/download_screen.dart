import 'package:flutter/cupertino.dart';

class DownloadScreen extends StatelessWidget {
  const DownloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DownloadScreen'),
    );
  }
}
